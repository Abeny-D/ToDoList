import { FaPlus } from "react-icons/fa";

const AddTask = () => {
    return (
        <div>
            <button className="btn btn-primary w-full rounded-2xl">Add new task <FaPlus /></button>

        </div>
    );
};

export default AddTask;
